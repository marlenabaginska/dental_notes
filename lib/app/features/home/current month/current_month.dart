import 'package:flutter/material.dart';

class CurrentMonth extends StatelessWidget {
  const CurrentMonth({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('blablablabalbal'),
    );
  }
}
